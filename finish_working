#/bin/sh
set -euo pipefail

if [ "$#" -lt 1 ]; then
    TICKET_ID=$(ticket);
else
    TICKET_ID=$1
fi

echo $TICKET_ID

jira_data $TICKET_ID | jq .summary
confirm ask_for_review
confirm jira_set_status $TICKET_ID in_review
