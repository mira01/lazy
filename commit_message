set -euo pipefail

if [ "$#" -lt 1 ]; then
    TICKET_ID=$(ticket);
else
    TICKET_ID=$1
fi

COMPONENT=$(component_name); jira_data $TICKET_ID | jq -r --arg component $COMPONENT '"\($component): \(.summary) #\(.key)"'
